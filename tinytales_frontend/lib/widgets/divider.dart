import 'package:flutter/material.dart';

class FlatDivider extends StatelessWidget {
  const FlatDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider();
  }
}
