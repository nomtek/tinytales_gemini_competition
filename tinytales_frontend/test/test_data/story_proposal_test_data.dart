import 'package:tale_ai_frontend/form/form.dart';

StoryProposal aStoryProposal({
  String plot = 'plot',
  String title = 'title',
}) =>
    StoryProposal(
      plot: plot,
      title: title,
    );
